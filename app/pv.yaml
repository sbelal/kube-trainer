apiVersion: v1
kind: PersistentVolume
metadata:
  name: hello-app-pv
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 100Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /tmp/hello-app-data
